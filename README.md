What you will learn from this paragraph:

*CSS Variables:*
1. Defining custom color variables.
2. Utilizing CSS variables.

*Global Box Sizing:*
1. Using the `*::before` and `*::after` selectors to apply a box-sizing reset.
2. Ensuring consistent box-sizing behavior and removing default padding that certain elements may have.

*Font Styling:*
1. Applying a specific font family.

*Selectors and Element Styling:*

1. Create basics style to elements.
